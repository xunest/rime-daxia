# 大侠输入法 使用手册

本手册中的每一项都已在本机配置中逐一核实。带 ⚠️ 的是需要额外操作才能用的。

---

## 一、切换与开关

| 操作 | 按键 |
|---|---|
| 中英文切换 | 轻按一下 `Shift`（不配合其他键） |
| 中英文切换（备用） | `Caps Lock` |
| 中文/英文标点切换 | `Ctrl+Shift+3` |
| 简体/繁体切换 | `Ctrl+Shift+4` |
| 打开方案选单（鼠标勾选各开关） | `Ctrl+` `` ` `` |
| 重新部署（改完配置后生效） | 菜单栏图标 → 重新部署 |

**例**：写代码时按 `Ctrl+Shift+3` 切到英文标点，逗号就输出 `,` 而不是 `，`。

`Ctrl+` `` ` `` 弹出的选单里还能切「全角/半角」和「辅码单字优先」，这两项没有独立快捷键。

---

## 二、打字时的编辑键

| 按键 | 作用 |
|---|---|
| `空格` | 上屏当前选中的候选词 |
| `1`~`7` | 直接选第 N 个候选词 |
| `-` / `=` | 上一页 / 下一页 |
| `Tab` / `Shift+Tab` | 光标移到下 / 上一个音节 |
| `Option+→` / `Option+←` | 同上 |
| `BackSpace` | 退格（撤销上次输入） |
| `Ctrl+BackSpace` | **删掉一整个音节** |
| `Delete` | 向后删除 |
| `Ctrl+Delete` | **删除或降权当前候选词** |
| `Esc` | 取消整段输入 |
| `Return` | 上屏原始拼音字母 |
| `Ctrl+Return` | 上屏转换后的拼音 |
| `Ctrl+Shift+Return` | 上屏注释内容 |
| `[` | 只取当前词的**第一个字** |
| `]` | 只取当前词的**最后一个字** |

**例 1 — 改错音节**：打 `zhongguoren` 发现第二个音节打错了，不必全删。按两下 `Tab` 把光标移到 `guo`，直接改。

**例 2 — 删一个音节**：打 `beijingdaxue` 想改成 `beijingdaxiao`，按 `Ctrl+BackSpace` 一次删掉整个 `xue`，比按 3 次退格快。

**例 3 — 以词定字**：想单独输入「侠」字，但打 `xia` 出来的第一候选是「下」。可以打 `daxia`（大侠），按 `]` 只上屏最后一个字「侠」。

**例 4 — 删掉记错的词**：某个错词老是排第一，选中它按 `Ctrl+Delete` 就能降权或删除。

**例 5 — 小键盘**：数字小键盘在输入状态下会正常输出数字，不会被当成选词键。

---

## 三、快捷输入（直接打这些字母）

| 打 | 出（示例） |
|---|---|
| `rq` | `2026-08-10` |
| `sj` | `20:15` |
| `xq` | `星期一` |
| `dt` | `2026-08-10T20:15:30+08:00` |
| `ts` | `1786709730`（Unix 时间戳） |
| `rqzh` | `二〇二六年八月十日` |
| `rqen` | `10 August 2026` |
| `nl` | `丙午马年六月廿七`（农历） |
| `uuid` | `f47ac10b-58cc-4372-...` |

**例**：写日志时打 `rq` 加空格，直接得到今天日期，不用去看日历。

---

## 四、算式与数字

**计算器** — 前缀 `cC` + 算式（注意：必须带 `cC` 前缀，直接打算式不会触发）：

```
cC1+2*3        → 7
cC(100-20)/4   → 20
cC3^2          → 9
cC2*pi         → 6.2831853071796
```

支持 `pi`、`e` 等常量。

**金额大写** — `R` + 数字：

```
R1234.56     → 壹仟贰佰叁拾肆元伍角陆分
R100         → 壹佰元
```

开发票、写合同用得上。

**数字后的标点** — 在数字后输入 `,` `.` `:` 并**紧接着再输数字**，才会连同数字一起直接上屏，不转成中文标点：

```
连续打 3.14      → 3.14   （不是 3。14）
连续打 12:30     → 12:30  （不是 12：30）
连续打 1,000     → 1,000
```

要点：符号后必须再跟数字才生效；**双击该符号**则恢复成中文标点映射。此功能需 librime ≥ 1.13.0，本机为 1.16.0，已支持。

---

## 五、符号库（`v` 开头）

打 `vhelp` 或 `vlist` 会列出全部 70 个分类。常用的：

| 打 | 内容 | 示例字符 |
|---|---|---|
| `vfh` | 杂项符号 | ☎ ☑ ☘ ☯ ♻ ⚡ ⚠ |
| `vdn` | 电脑键盘符 | ⌘ ⌃ ⌥ ⇧ ⇪ ␣ ⇥ ↩ ⌫ ⎋ |
| `vjt` | 箭头 | ← → ↑ ↓ ⇒ ⇔ |
| `vsx` | 数学 | ± × ÷ ≠ ≤ ≥ ∞ ∑ √ |
| `vszq` | 圆圈数字 | ① ② ③ ④ ⑤ |
| `vsb` / `vxb` | 上标 / 下标 | ¹ ² ³ / ₁ ₂ ₃ |
| `vxl` / `vxld` | 希腊字母小写/大写 | α β γ / Α Β Γ |
| `vdw` | 单位 | ℃ ㎡ ㎏ ㎜ |
| `vhb` | 货币 | ¥ $ € £ ₩ |
| `vbd` | 标点 | 、 。 ； ： 《 》 |
| `vfs` | 分数 | ½ ⅓ ¼ ⅔ |
| `vlm` / `vlmd` | 罗马数字 | ⅰ ⅱ ⅲ / Ⅰ Ⅱ Ⅲ |
| `vpy` | 带调拼音（全部） | ā á ǎ à ō ó ǒ ò ü ǖ |
| `va` `vo` `ve`… | 单个韵母的带调形式 | 打 `va` → ā á ǎ à |
| `vxz` / `vxzm` | 星座符号 / 名称 | ♈ ♉ / 白羊座 金牛座 |
| `vjq` | 节气 | 立春 雨水 惊蛰 |
| `vkx` | 康熙部首 | ⼀ ⼁ ⼂ |

**例 1 — 写快捷键说明**：打 `vdn`，选 `⌘`，得到「按 ⌘S 保存」。

**例 2 — 声调标注**：打 `va` 得到 `ā á ǎ à`（一二三四声顺序），标注拼音很方便。

**例 3 — 摄氏度**：打 `vdw` 找 `℃`，比打「摄氏度」再删字快。

---

## 六、Emoji

已默认开启。正常打词，候选里会附带表情：

```
打 kaixin    → 开心 😄
打 xihuan    → 喜欢 ❤️
打 zan       → 赞 👍
```

不想要时在 `Ctrl+` `` ` `` 选单里关掉 emoji。

---

## 七、生僻字与疑难字

### 部件拆字反查 `uU` + 拼音

不确定字怎么打时，用它按拼音列出同音字（含大量生僻字）：

```
uUhao   → 列出所有 hao 音的字
uUqi    → 列出所有 qi 音的字
```

⚠️ **注意**：如果候选里出现很多 `?` 方块，说明缺少显示扩展区汉字的字体。那些是 Unicode 扩展区的极生僻字，检索本身是正确的，只是画不出来。

**本机已安装花园明朝体（HanaMinA + HanaMinB），这些字可以正常显示。**

若在新机器上遇到方块，装花园明朝体即可：

1. 从 GlyphWiki 官方源下载：`https://glyphwiki.org/hanazono/hanazono-20170904.zip`
2. 解压得到 `HanaMinA.ttf` 与 `HanaMinB.ttf`，双击安装（或拷进 `~/Library/Fonts/`）
3. 重新部署输入法

雾凇皮肤已在 `font_face` 里指定了 `HanaMinB` 作为备用字体，装上就自动生效，无需改配置。不装也不影响正常打字——常用字都能显示。

### 辅码检字 `` ` ``

同音字太多翻页麻烦时，用部件筛选。引导符是反引号 `` ` ``。

⚠️ 具体用法依赖 `search.lua`，其文档在 [rime-radical-pinyin](https://github.com/mirtlecn/rime-radical-pinyin) 仓库，我没有实测过，此处不细写以免误导。

### Unicode 码位 `U` + 十六进制

```
U4F60   → 你
U2603   → ☃
```

---

## 八、让常用内容一键输入（最实用）

编辑 `~/Library/Rime/custom_phrase.txt`，这里的词**权重最高，永远排第一**。

格式：`词汇<Tab>编码`（中间是 Tab，不是空格）

```
zhang@example.com	yx
13800138000	sjh
广东省深圳市南山区科技园	dz
收到，我看一下	sd
https://github.com/myname	gh
```

存盘后重新部署。之后打 `yx` 直接出邮箱，打 `dz` 出地址。

⚠️ **两条重要限制**（文件头的官方提醒）：

1. **编码不要用完整拼音**。比如别用 `youxiang` 做邮箱的编码，否则「邮箱」这个词无法参与造句，自造词也记不住。用 `yx` 这种缩写。
2. 如果目的是「让某个拼音下的某个候选词排第一」，应该改方案里的 `pin_cand_filter`，不要用这个文件。

**当前已内置的置顶**：`d`→的、`m`→吗、`hm`→后面。

---

## 九、自动生效的排序优化（无需配置）

这些已在后台工作：

- **长词优先** — 输入 `jie`，会把 2 个长词插到第 4、5 位，如「饥饿」「极恶」
- **英文降权** — 约 400 个短单词（`rug` `bus` `dim` `bad`…）被压到第 2 位，避免抢占拼音候选。输入 `rug` 得到「1.如果 2.rug」而非「1.rug」
- **错音错字提示** — 打错音也能出正确候选，并提示正确读音
- **模糊音** — 已开启平翘舌不分（`zh/z` `ch/c` `sh/s`）和 `n/l` 不分。打 `zongguo` 也能出「中国」
- **万象语法模型** — 按上下文调整同音词排序
- **智能调频** — 常用词自动前排，记在 `~/Library/Rime/rime_ice.userdb`

---

## 十、设置界面

菜单栏「大侠输入法」→ 用户设定，或打开「大侠输入法」App。

- **外观** — 皮肤、配色、自定义取色
- **输入设置** — 候选词数量、字号、字体、横竖排、圆角、透明度、模糊音、标点行为、语法模型
- **常用语** — 编辑自定义短语
- 改完点「应用并部署」生效

「恢复默认」会还原到出厂配置。

---

## 附：本机当前配置

- 方案：雾凇拼音（全拼）
- 候选词：7 个，横排
- 字号 21，序号 20
- 模糊音：平翘舌不分、n/l 不分
- 标点：中文标点；输出简体；Emoji 开
- 万象语法模型：开
- 生僻字字体：花园明朝体 HanaMinA / HanaMinB（已装）
